// Layout minimal pour /update-password — pas de header/footer public.
// La page est standalone : fond sombre, fullscreen.
export default function UpdatePasswordLayout({ children }) {
  return children;
}
